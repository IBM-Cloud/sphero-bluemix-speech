---
declared-services:
  speech-to-text-service-standard:
    label: anki-speech
    plan: standard
applications:
- name: anki-speech
  path: .
  command: npm start
  memory: 512M
  services:
  - anki-speech
  env:
    NODE_ENV: production
    SECURE_EXPRESS: 1
